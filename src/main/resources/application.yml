spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/controle_presenca
    username: controle_user
    password: 123456
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

server:
  port: 8080

logging:
  level:
    org.hibernate.SQL: debug
    org.hibernate.type.descriptor.sql.BasicBinder: trace

app:
  admin:
    user: admin
    pass: admin123